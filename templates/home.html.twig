{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur le meilleur réseau de location !{% endblock %}

{% block header %}
<h2>Voici le header</h2>
{% endblock %}

{% block body %}

<div class="container">
   <div class="jumbotron mt-3">
        <h1 class="display-3">Bienvenue sur SymBNB</h1>
        <p class="lead">Nous mettons en relation les voyageurs et les proprétaires qui veulent profiter d'opportunités et gagner de l'argent en proposant leurs biens à la location.</p>
        <hr class="my-4">
        <h4>Vous êtes voyageur et recherchez le lieu idéal pour votre prochaine escapade ?</h4>
        <p class="lead">
            <a class="btn btn-primary btn-lg mt-3" href="{{ path('ads_index') }}" role="button">Voir nos annonces</a>
        </p>
    </div>

<h2 class="h1 my-5">Nos locations stars</h2>

<div class="row">
    {% for data in ads %}
        {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
    {% endfor %}
</div>

<h2 class="h1 my-5">Nos propiétaires stars</h2>

<div class="row">
    {% for data in users %}
    <div class="col">
        <div class="card border-secondary mb-3">
            <div class="card-body">
                <h4 class="card-title">{{data.user.fullName}}</h4>

                <hr>

                <div class="card-text">
                    <img src="{{data.user.picture}}" alt="Avatar de {{data.user.fullName}}" class="float-left mr-3">
                    <p>{{data.user.introduction}}</p>
                    <a href="{{ path('user_show', {'slug': data.user.slug}) }}" class="btn btn-success float-right">En savoir plus</a>
                </div>

            </div>
        </div>
    </div>
    {% endfor %}
</div>


</div>
{% endblock %}